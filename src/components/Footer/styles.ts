import styled from "styled-components";

export const Footer = styled.footer`
  display: flex;
  gap: 0.4rem;
  max-width: 144rem;
  padding: 0 15.6rem;
  margin: 0 auto 2.1rem;

  p {
    font-size: 1.4rem;
    font-weight: 500;
  }

  .nome {
    color: #9bf00b;
    font-weight: 500;
  }
`;
